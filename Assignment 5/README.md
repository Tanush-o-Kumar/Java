Design a banking application with the following class definitions: Customer, Account, LoanAccount(extends Account), and SavingsAccount(extends Account). Define the methods deposit(), withdraw() in the base class Account and override them in the child classes as required.

In the main class, create arraylist of customers, accounts and display consolidated information of each customer's accounts.
