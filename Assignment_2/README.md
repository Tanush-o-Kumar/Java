Create a Vehicle class. Add some public and private data members. Define getter and setter methods for private data members.

Define 3 types of Constructors: Default, parameterized and copy.

Define the following methods: start(), stop(), drive(), calcMileage(), changeSpeed().

Create a separate class for main method.

Create multiple Vehicle objects.

Create an array of Vehicles.

Print the following values in tabular format for each vehicle:

Brand Name, Model Name, Price, Mileage.
