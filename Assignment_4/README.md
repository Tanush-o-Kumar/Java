Define a Vector Class. The constructor should check for the dimensions and should allow only 2D or 3D vector creation. If not 2D or 3D, the constructor should throw a VectorException.

Define methods: add(), subtract(), dotProduct().

First 2 methods return a Vector object whereas the third method returns a single value.

Add a method to check dimensions of the vectors to be added, subtracted to used for dot product. The method should throw VectorException if the dimensions are not the same.

Add a method to display the resultant vector.
